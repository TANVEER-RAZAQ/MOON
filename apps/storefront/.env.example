# Backend API URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# On-demand revalidation secret (must match backend REVALIDATE_SECRET)
REVALIDATE_SECRET=your-revalidation-secret-here
